/db/
/incremental_db/
/output_files/
/sim_output/
/snap/
/transcript
/*.rpt
**/*.rpt
/*.summary
/*.smsg
/*.jdi
/*.sof
/*.rbf
/*.qws
/roms/
*.zip
*.7z
*.img
*.chd
*.bin
*.key
!rtl/cpu/fx68k/*.mem

# Local simulator/checkpoint and deployment output.
/.build/
/.tmp_verilator/
/*.vltsv
/*.vltsv.token
/cfg/
/nvram/
/screenshots/
/release_mister/
/c5_pin_model_dump.txt
/cr_ie_info.json
/jtag.cdf
/modelsim.ini
/quartus-*.log

# Standard MiSTer template build outputs.
/build_id.v
/*.cdf
*.qdf
*.qws
*.ppf
*.ddb
*.csv
*.cmp
*.sip
*.spd
*.bsf
*.sopcinfo
*.f

# Local MRA staging/regeneration output (tools/gen_mra.py --out-dir default).
# Curated release MRAs -- the ones actually meant to be published -- live in
# /releases/ instead and are tracked normally.
/mra/

# Keep the eight curated release MRAs visible to Git. Other clone experiments
# and local generator output remain ignored here and under mra/ above.
/releases/*.mra
!/releases/Bonanza Bros.mra
!/releases/Crack Down.mra
!/releases/Gain Ground (Japan, 2 Players, Floppy Based, FD1094 317-0058-03b).mra
!/releases/Gain Ground.mra
!/releases/Hot Rod (Japan, 4 Players).mra
!/releases/Hot Rod (World, 3 Players, Turbo).mra
!/releases/Rough Racer.mra
!/releases/Scramble Spirits.mra

# Local-only development material. The published repository follows the
# Template_MiSTer layout: core source (rtl/, sys/, the Quartus project files,
# files.qip), releases/, README.md and LICENSE. Everything below stays on the
# development machine.
/artifacts/
/verif/
/tools/
/docs/
/scenarios/
/24/
/.mister/
/.mame_mcp/
/.playwright-mcp/
/.codex-mister-build/
/.claude/
/AGENTS.md
/NOTES.md
/core-debug.toml
